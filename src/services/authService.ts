export const authService = {
  async login(email: string, _password: string) { void email; void _password; return null as any; },
  async logout() {},
  async forgotPassword(email: string) { void email; },
};

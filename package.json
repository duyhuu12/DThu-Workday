{
  "name": "dthu-workday-full-system",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "install:all": "npm install --prefix FE && npm install --prefix BE",
    "typecheck": "npm run typecheck --prefix FE && npm run typecheck --prefix BE",
    "build": "npm run build --prefix FE && npm run build --prefix BE",
    "prisma:generate": "npm run prisma:generate --prefix BE",
    "prisma:migrate": "npm run prisma:migrate --prefix BE",
    "prisma:seed": "npm run prisma:seed --prefix BE"
  }
}
